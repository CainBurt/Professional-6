{# {% extends "base.twig" %}
{% block content %}

            <div class="inner overlay"></div>
            <div class="inner registation-form">
                <div class="inner close-btn">
                    {{ source(directory ~ '/src/img/close.svg') }}
                </div>

                {% for ses in post_sessions %}
                    {% set contact_f = ses.form_cf7 %}

                    <div class="cf-forms form-{{ loop.index }}">
                        {{ contact_f }}
                    </div>
                {% endfor %}
            </div>

        

   

            <section data-scroll-section class="inner-page">
                
                <div data-scroll data-scroll-speed="-2.25" data-scroll-delay=".06" class="left-ball">
                    <img src="{{ theme.link ~ "/src/img/b1.png"}}" alt="Nike Footballverse">
               </div>


                <div class="container intro">
                    {% set title = post.get_field('event_title') %}
                    {{ title }}
                </div>
            

                <div class="container event-container">
                    <div class="row g-2">
                    {% for ses in post_sessions %}
                        
                        {% set event_description = ses.event_description  %}                
                        {% set event_img = TimberImage(ses.event_image) %}
                        {% set event_alt = event_img.title ? event_img.title : event_img.alt %}


                        <div class="col-md-6 block block-{{ loop.index }}">

                            <div  class="img-detect" data-scroll>
                                <img class="img-fluid d{{ loop.index }}" src="{{ event_img }}" alt="{{ event_alt }}">
                            </div>
                            

                            <div class="event-desc mx-auto appear-y d-{{ loop.index }}" data-scroll-offset="10%" data-scroll>
                                {{ event_description }}

                                {% if ses.register_btn %}
                                    <button class="{{ ses.register_btn}} register-button">
                                        {{ options.register_btn }}
                                    </button>
                                {% else %}
                                    <h3 class="coming soon pt-5 pt-xl-5"> Fully Booked. </h3>
                                {% endif %}

                            </div>
                        </div>
                    
                     {% endfor %}
                    </div>
                </div>
                

                {% include "/components/footer.twig" %}    
                
                <div data-scroll data-scroll-speed="-2.5" class="right-ball">
                    <img src="{{ theme.link ~ "/src/img/b2.png"}}" alt="Nike Footballverse">
                </div>
            </section>

{% endblock %} #}

{% extends "base.twig" %}
    {% block content %}

        <div class="overlay"></div>
        <div class="creator registation-form">
            <div class="creator close-btn">
                {{ source(directory ~ '/src/img/close.svg') }}
            </div>
            {% for ses in post_sessions %}
                {% set contact_f = ses.form_cf7 %}
                <div class="creator-form">
                    {{ contact_f }}
                </div>
            {% endfor %}
        </div>


        <section data-scroll-section class="position-relative" style="background-color: #e6dace">

            {% for ses in post_sessions %}
                        
            {% set pdesc = ses.event_description  %}                
            {% set pimg = TimberImage(ses.event_image) %}
            {% set pimg_alt = pimg.title ? pimg.title : pimg.alt %}


                <div class="container ps-0 pb-container">
                        {# <div class="row"> #}
                        <div class="col-xl-6 has-bg" style="background-image:url('{{ pimg }}');">
                        
                        </div>

                        {# <div class="col-xl-6 ps-xl-8 pb-content my-auto">                       
                                <div  class="appear-y d1" data-scroll>
                                    {{ pdesc }}
                                </div>

                                <div class="appear-y d2" data-scroll>
                                    <div class="d-inline-flex flex-column">
                                        <button class="register-button">
                                            {{ options.register_btn }}
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>                #}
                </div>
           {% endfor %}

           {% include "/components/footer.twig" %}  

            {#  #}
           
      </section>

    {% endblock %}